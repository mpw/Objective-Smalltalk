#!/usr/local/bin/stsh
#-<void>fileserver:<ref>dir
context loadFramework:'MPWSideWeb'
server := MPWSchemeHttpServer new.
server server setPort:8081.
server start:nil.


# server setScheme: scheme:file 
# server setScheme: dir asScheme.
 server setScheme: (dir asScheme cachedBy: MPWSiteMap scheme).

shell runInteractiveLoop

