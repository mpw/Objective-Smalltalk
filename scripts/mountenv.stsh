#!/usr/local/bin/stsh
framework:SchemeFUSE load.

theScheme := scheme:env .
 
fs := MPWSchemeFilesystem alloc initWithScheme: theScheme.
fs mountAtPath:'/Volumes/SchemeFS'.


shell runInteractiveLoop

fs unmount.
stdout println:'unmounted'.
