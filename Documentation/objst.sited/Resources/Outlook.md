
Objective-Smalltalk has been used successfully as a plugin language, for creating web sites and 



Work is currently focused on applying Objective-Smalltalk in different contexts, so building full applications
with user interfaces, including tooling and librray components requires to make this happen.

There is a native compiler based on LLVM that can compile some simple Objective-Smalltalk classes.
This needs to be extended, particulaly to read C/Objective-C/Swift API information.

